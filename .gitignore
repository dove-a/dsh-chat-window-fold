# 依赖
node_modules/

# 本地 pnpm lockfile（含 link: 本地路径，勿提交；CI 用 package.json 直接安装）
pnpm-lock.yaml

# npm 认证配置（含令牌引用，禁止提交）
.npmrc

# 构建产物与本地日志
*.tgz
*.log
.DS_Store
coverage/

# 本地测试痕迹
gui-test-screenshots/