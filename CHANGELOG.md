# Changelog

本文件以使用者视角记录 dsh-chat-window-fold 的版本变更（内部实现细节见开发文档）。

## [0.1.5] - 2026-08-15

- **修复**：贴顶加载更稳——加载器异常卡住时 8 秒自动解除（不再卡死"贴顶不加载"）；锚定增强，锚定行不可用时按新内容高度自动校正视口，画面不易晃动。
- **修复**：会话打开在底部时，初始折叠正确生效（此前在部分打开场景下首轮折叠可能不触发）。
- **行为**：折叠判定节奏严格化——判定点到来时若不在底部则跳过该轮（不再补执行），行为更可预期。

## [0.1.4] - 2026-08-15

- **修复**：隐藏系统 "Load earlier" 按钮的文本匹配改为官方字典标签（"Load earlier" / "加载更早" / "載入更早"，前缀匹配）——界面换行/省略的按钮也能正确隐藏；并加防误伤保护，含 token/压缩/compact 字样的按钮（上下文压缩披露等）一律不隐藏。
- 行为与配置无变化。

## [0.1.3] - 2026-08-15

- **修复**：折叠发生时视口自动回锚到新的底部（画面内容不变，仍显示同一批尾部消息）——避免折叠后视口滞留在窗口中部导致后续折叠停摆。
- 行为与配置无变化；补充说明插件边界（不写会话快照、不卸载 DOM 行、真 token 压缩在宿主侧）。

## [0.1.2] - 2026-08-15

- **修复**：折叠逻辑在部分场景可能未实际触发——现已修正，滚动到底部时早期消息会如期折叠，贴顶可逐批追回。
- 行为与配置均无变化（阈值/判定周期仍为 `foldThreshold=50`、`foldCheckEvery=25`）。

## [0.1.1] - 2026-08-15

- 文档与元数据更新（README 改为使用者说明、描述精简），插件行为无变化。

## [0.1.0] - 2026-08-15

- 首版功能：
  - **自动折叠**：滚动到底部时，窗口外的早期消息自动收起，窗口保持约 50 条（低配机器友好）；
  - **贴顶展开**：滚动到最顶部逐批带回更早消息（每次最多 50 条），画面位置保持不动，直到没有更多历史；
  - **多会话隔离**：各会话折叠状态互不影响，历史数据只读完好；
  - **系统按钮隐藏**：激活后隐藏 "Load earlier" 按钮；
  - **可配置**：行 config `foldThreshold` / `foldCheckEvery`。